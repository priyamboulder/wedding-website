// ── /dashboard/milestones route ────────────────────────────────────────
// Full timeline of every milestone the couple has hit — both active and
// dismissed. Reachable from the dashboard's milestone marker via
// "View all milestones".

import { MilestonesTimeline } from "@/components/dashboard/MilestonesTimeline";

export default function MilestonesPage() {
  return <MilestonesTimeline />;
}
